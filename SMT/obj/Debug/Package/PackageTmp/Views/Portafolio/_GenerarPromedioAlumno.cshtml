@model SMT.Models.DB.Portafolio.ImpresionHeaderPortafolio

@if (Model.portafolios.Where(i => i.TipoPortafolio.Nombre == Model.tipo).Count() > 0)
{
    foreach (SMT.Models.DB.Portafolio port in Model.portafolios.Where(i => i.TipoPortafolio.Nombre == Model.tipo))
    {

        if (port.Activo1 == true)
        {
            <td class="text-center">@(SMT.Models.Util.calificacionSEP10String( port.PortafolioAlumno.ToList().Select(i => float.Parse(i.Aspecto1)).Average()))</td>
        }
        if (port.Activo2 == true)
        {
            <td class="text-center">@(SMT.Models.Util.calificacionSEP10String(port.PortafolioAlumno.ToList().Select(i => float.Parse(i.Aspecto2)).Average()))</td>
        }
        if (port.Activo3 == true)
        {
            <td class="text-center">@(SMT.Models.Util.calificacionSEP10String(port.PortafolioAlumno.ToList().Select(i => float.Parse(i.Aspecto3)).Average()))</td>
        }
        if (port.Activo4 == true)
        {
            <td class="text-center">@(SMT.Models.Util.calificacionSEP10String(port.PortafolioAlumno.ToList().Select(i => float.Parse(i.Aspecto4)).Average()))</td>
        }
        if (port.Activo5 == true)
        {
            <td class="text-center">@(SMT.Models.Util.calificacionSEP10String(port.PortafolioAlumno.ToList().Select(i => float.Parse(i.Aspecto5)).Average()))</td>
        }
        <td></td>
    }
}